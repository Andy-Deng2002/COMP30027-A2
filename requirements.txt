# -----------------------------------------------------------------
# Final, minimal set of dependencies for deploying app.py
# All versions are pinned where possible to speed up deployment.
# -----------------------------------------------------------------

# Core app framework and data handling
streamlit==1.37.1
pandas==2.2.2
numpy==1.26.4
requests==2.32.3

# Machine Learning and Metrics
scikit-learn==1.5.1
xgboost # Required for loading XGBoost results, let pip resolve version

# Image processing and plotting
Pillow==10.4.0
matplotlib # For st.pyplot, let pip resolve version
opencv-python # A dependency for grad-cam and other image operations

# PyTorch and Grad-CAM (CPU-only for cloud compatibility)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
grad-cam

# For Sankey plot from GitHub
git+https://github.com/Pierre-Sassoulas/pySankey.git 